export const CartReducer = (state, action) => {
    switch(action.type) {
        case "AddToCart": {
            return {
                const item = state.find((i) => i.id === action.payload)
            } 
        }
    }
}